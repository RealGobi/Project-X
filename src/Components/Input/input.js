import React from 'react';
import PropTypes from 'prop-types';

import '../../style/main.scss';

const Input = () => (
  <div className="input">
    <input type="text" />
  </div>
);

export default Input;
